import { calcular, saludar, sumar } from "./funciones.js";

saludar("Cristián", "Frías")

sumar(4, 1, 2, 8, 10, 9, 15, 20, {id:10}) // estructura de arreglo o array, {objeto dentro de un arreglo}

const resultado = sumar(4, 1, 2, 8, 10, 9, 15, 20) 
console.log(resultado)

calcular("sumar", 4, 5, 6, 9) // sumatoria de números de un arreglo